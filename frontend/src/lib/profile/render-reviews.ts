import { createIslandRenderer } from '../shared/mount-island';
import { ReviewsSection } from '../../components/profile/ReviewsSection';

export const renderReviews = createIslandRenderer(ReviewsSection);
