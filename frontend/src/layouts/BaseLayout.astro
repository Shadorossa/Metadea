---
import '../styles/core/global.css';
import '../styles/components/components.css';
import '../styles/components/navbar.css';
import '../styles/components/footer.css';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import type { Locale, Translations } from '../i18n/index';
import { useTranslations } from '../i18n/index';
import ClientRouter from 'astro/components/ClientRouter.astro';

import nebulaUrl from '../styles/core/themes/nebula.css?url';
import auroraUrl from '../styles/core/themes/aurora.css?url';
import cosmosUrl from '../styles/core/themes/cosmos.css?url';
import emberUrl from '../styles/core/themes/ember.css?url';
import oceanUrl from '../styles/core/themes/ocean.css?url';
import sakuraUrl from '../styles/core/themes/sakura.css?url';
import cyberpunkUrl from '../styles/core/themes/cyberpunk.css?url';
import newspaperDarkUrl from '../styles/core/themes/newspaper-dark.css?url';
import pipboyUrl from '../styles/core/themes/pipboy.css?url';
import glassmorphismUrl from '../styles/core/themes/glassmorphism.css?url';
import synthwaveUrl from '../styles/core/themes/synthwave.css?url';
import forestUrl from '../styles/core/themes/forest.css?url';

const themeUrls = {
  nebula: nebulaUrl,
  aurora: auroraUrl,
  cosmos: cosmosUrl,
  ember: emberUrl,
  ocean: oceanUrl,
  sakura: sakuraUrl,
  cyberpunk: cyberpunkUrl,
  'newspaper-dark': newspaperDarkUrl,
  pipboy: pipboyUrl,
  glassmorphism: glassmorphismUrl,
  synthwave: synthwaveUrl,
  forest: forestUrl,
};

interface Props {
  title: string;
  lang: Locale;
  activePage?: 'home' | 'search' | 'local' | 'notifications' | 'profile' | 'tier';
}

const { title, lang, activePage } = Astro.props;
const t = useTranslations(lang);
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <ClientRouter />
    <link id="theme-stylesheet" rel="stylesheet" href="" transition:persist />
    <!-- Apply theme before CSS loads to prevent flash -->
    <script is:inline define:vars={{ themeUrls }}>
      function applyTheme() {
        var t = localStorage.getItem('app_theme') || 'nebula';
        document.documentElement.setAttribute('data-theme', t);
        var link = document.getElementById('theme-stylesheet');
        if (link) link.setAttribute('href', themeUrls[t] || themeUrls['nebula']);
      }
      applyTheme();
      // Re-apply theme after each View Transitions navigation
      document.addEventListener('astro:after-swap', applyTheme);
      window.__updateTheme = function(newTheme) {
        localStorage.setItem('app_theme', newTheme);
        applyTheme();
      };
    </script>
  </head>
  <body data-page={activePage}>
    <div id="app-bg" aria-hidden="true" transition:persist></div>
    <Navbar activePage={activePage} i18n={t.nav} />
    <div id="page-content" transition:name="page-content">
      <slot />
    </div>
    <Footer lang={lang} />

    <!-- Auth modal -->
    <dialog id="auth-modal" class="auth-modal">
      <div class="auth-modal-box">

        <p class="modal-title">Crea tu perfil local</p>

        <form id="modal-login-form" style="display: flex; flex-direction: column; gap: 0.75rem; width: 100%;">
          <input 
            type="text" 
            id="modal-username-input" 
            class="input-dark" 
            placeholder="Nombre de usuario" 
            required 
            minlength="3" 
            maxlength="20"
          />
          <button type="submit" class="btn btn--primary" style="justify-content: center; width: 100%;">
            Comenzar
          </button>
        </form>

        <!-- Close -->
        <button class="auth-close" id="close-auth-modal" aria-label="Cerrar">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
            <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>
      </div>
    </dialog>

    <script>
      // Auth modal — runs on first load and after each View Transitions swap
      function initAuthModal() {
        const modal     = document.getElementById('auth-modal') as HTMLDialogElement | null;
        const closeBtn  = document.getElementById('close-auth-modal');
        const modalForm = document.getElementById('modal-login-form');
        const modalInput = document.getElementById('modal-username-input') as HTMLInputElement | null;

        closeBtn?.addEventListener('click', () => modal?.close());
        modal?.addEventListener('click', (e) => { if (e.target === modal) modal?.close(); });

        modalForm?.addEventListener('submit', async (e) => {
          e.preventDefault();
          const username = modalInput?.value.trim();
          if (username) {
            try {
              const { storeAuthToken } = await import(/* @vite-ignore */ '../lib/tauri');
              await storeAuthToken('offline_token', username);
              window.location.reload();
            } catch {
              console.error('Error storing token');
            }
          }
        });
      }
      
      let updaterChecked = false;

      async function checkAppUpdates() {
        if (typeof window === 'undefined' || !('__TAURI_IPC__' in window)) return;
        if (!navigator.onLine) return;
        try {
          const { check } = await import('@tauri-apps/plugin-updater');
          const { relaunch } = await import('@tauri-apps/plugin-process');

          const update = await check();
          if (update && update.available) {
            const confirmUpdate = confirm(
              `¡Nueva versión ${update.version} disponible!\n\n¿Quieres descargarla e instalarla automáticamente ahora?`
            );
            if (confirmUpdate) {
              await update.downloadAndInstall();
              await relaunch();
            }
          }
        } catch (error) {
          console.warn("[Updater] No se pudo comprobar actualizaciones:", error);
        }
      }

      document.addEventListener('astro:page-load', () => {
        initAuthModal();
        if (!updaterChecked) {
          updaterChecked = true;
          setTimeout(checkAppUpdates, 2000);
        }
      });
    </script>
  </body>
</html>
