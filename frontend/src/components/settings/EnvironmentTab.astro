---
import { ICON_HELP_CIRCLE } from "../../lib/shared/icon-strings";
import type { Translations } from "../../i18n/index";

interface Props {
  t: Translations;
}
const { t } = Astro.props;
---

<!-- Tab: Entorno -->
<div class="settings-panel hidden" id="panel-environment">
  <h2 class="settings-panel-title">{t.settings.tab_environment}</h2>

  <div class="settings-section">
    <h3 class="settings-section-title">{t.settings.env_title}</h3>
    <p class="settings-hint">{t.settings.env_hint}</p>
    <p class="settings-hint" style="margin-top: 0.35rem;">
      Estas claves conectan Metadea con cada plataforma para poder buscar y sincronizar contenido. Son <strong>opcionales</strong>: sin ellas, la búsqueda de ese tipo de contenido (videojuegos, películas/series, etc.) simplemente no funcionará hasta que las añadas. Pulsa el icono de cada plataforma para configurarla, y el botón de ayuda (?) si no sabes de dónde sacar la clave.
    </p>
    <button
      class="btn btn--sm btn--ghost"
      id="open-env-folder-btn"
      style="margin: 0.75rem 0 1rem;"
    >
      📁 {t.settings.env_open_folder}
    </button>

    <!-- API Platforms Selector Bar -->
    <div class="api-platforms-selector">
      <div class="api-platform-item">
        <button type="button" class="api-platform-tab-btn" data-platform="igdb" title="IGDB (Videojuegos)">
          <img class="api-platform-logo" src="/API/IGDB_logo.png" alt="IGDB" />
        </button>
        <span class="api-platform-label">Videojuegos</span>
      </div>
      <div class="api-platform-item">
        <button type="button" class="api-platform-tab-btn" data-platform="tmdb" title="TheMovieDB (Cine & Series)">
          <img class="api-platform-logo" src="/API/Tmdb.new.logo.png" alt="TMDB" />
        </button>
        <span class="api-platform-label">Cine y series</span>
      </div>
      <div class="api-platform-item">
        <button type="button" class="api-platform-tab-btn" data-platform="steam" title="Steam (Sincronización PC)">
          <img class="api-platform-logo" src="/platforms/steam_logo.png" alt="Steam" />
        </button>
        <span class="api-platform-label">Steam</span>
      </div>
      <div class="api-platform-item">
        <button type="button" class="api-platform-tab-btn" data-platform="anilist" title="AniList (Anime & Manga)">
          <img class="api-platform-logo" src="/API/Anilist_logo.png" alt="AniList" />
        </button>
        <span class="api-platform-label">Anime y manga</span>
      </div>
    </div>

    <!-- API Keys Safe Content Viewport -->
    <div class="api-keys-viewport">

      <!-- State: None selected (Default) -->
      <div class="api-keys-empty-placeholder" id="api-keys-placeholder">
        <span class="api-keys-placeholder-icon">🔑</span>
        <h4>Elige una plataforma arriba</h4>
        <p>Cada icono es un servicio distinto (videojuegos, cine/series, Steam, anime/manga). Al pulsarlo verás qué claves necesita y un botón de ayuda (?) para conseguirlas paso a paso.</p>
      </div>

      <!-- Form: IGDB -->
      <div class="api-platform-form-block hidden" id="api-form-igdb">
        <div class="env-service-header" style="position: relative; margin-bottom: 1.25rem;">
          <span class="api-form-service-title">Twitch / IGDB API</span>
          <button type="button" class="settings-help-btn" data-help="igdb" title="¿Cómo obtener las claves de IGDB?" style="position: absolute; top: 0; right: 0;">
            <Fragment set:html={ICON_HELP_CIRCLE} />
          </button>
        </div>
        <div class="env-fields env-fields--row">
          <div class="env-field">
            <label class="env-label" for="igdb-client-id">{t.settings.env_igdb_client_id}</label>
            <input
              class="env-input"
              type="text"
              id="igdb-client-id"
              placeholder="xxxxxxxxxxxxxxxxxxxxxxxx"
              autocomplete="off"
              spellcheck="false"
            />
          </div>
          <div class="env-field">
            <label class="env-label" for="igdb-client-secret">{t.settings.env_igdb_client_secret}</label>
            <input
              class="env-input"
              type="password"
              id="igdb-client-secret"
              placeholder="••••••••••••••••••••••••"
              autocomplete="off"
            />
          </div>
        </div>
      </div>

      <!-- Form: TMDB -->
      <div class="api-platform-form-block hidden" id="api-form-tmdb">
        <div class="env-service-header" style="position: relative; margin-bottom: 1.25rem;">
          <span class="api-form-service-title">TheMovieDB API</span>
          <button type="button" class="settings-help-btn" data-help="tmdb" title="¿Cómo obtener las claves de TMDB?" style="position: absolute; top: 0; right: 0;">
            <Fragment set:html={ICON_HELP_CIRCLE} />
          </button>
        </div>
        <div class="env-fields env-fields--row">
          <div class="env-field">
            <label class="env-label" for="tmdb-access-token">{t.settings.env_tmdb_access_token}</label>
            <input
              class="env-input"
              type="password"
              id="tmdb-access-token"
              placeholder="••••••••••••••••••••••••••••••••"
              autocomplete="off"
            />
          </div>
          <div class="env-field">
            <label class="env-label" for="tmdb-api-key">{t.settings.env_tmdb_api_key}</label>
            <input
              class="env-input"
              type="password"
              id="tmdb-api-key"
              placeholder="••••••••••••••••••••••••••••••••"
              autocomplete="off"
            />
          </div>
        </div>
      </div>

      <!-- Form: Steam -->
      <div class="api-platform-form-block hidden" id="api-form-steam">
        <div class="env-service-header" style="position: relative; margin-bottom: 1.25rem;">
          <span class="api-form-service-title">Steam Web API</span>
          <button type="button" class="settings-help-btn" data-help="steam" title="¿Cómo obtener la clave de Steam?" style="position: absolute; top: 0; right: 0;">
            <Fragment set:html={ICON_HELP_CIRCLE} />
          </button>
        </div>
        <div class="env-fields">
          <div class="env-field">
            <label class="env-label" for="steam-api-key">{t.settings.env_steam_api_key}</label>
            <input
              class="env-input"
              type="password"
              id="steam-api-key"
              placeholder="••••••••••••••••••••••••••••••••"
              autocomplete="off"
            />
          </div>
        </div>
      </div>

      <!-- Form: AniList -->
      <div class="api-platform-form-block hidden" id="api-form-anilist">
        <div class="env-service-header" style="position: relative; margin-bottom: 1.25rem;">
          <span class="api-form-service-title">AniList Developer API</span>
          <button type="button" class="settings-help-btn" data-help="anilist" title="¿Cómo obtener la clave de AniList?" style="position: absolute; top: 0; right: 0;">
            <Fragment set:html={ICON_HELP_CIRCLE} />
          </button>
        </div>
        <div class="env-fields">
          <div class="env-field">
            <label class="env-label" for="anilist-client-id">Client ID</label>
            <input
              class="env-input"
              type="text"
              id="anilist-client-id"
              placeholder="Ej. 18343"
              autocomplete="off"
            />
          </div>
        </div>
      </div>

    </div>

    <div class="settings-divider settings-divider--actions" style="justify-content: center; margin-top: 1.5rem;">
      <button class="btn btn--primary" id="env-save-btn"
        >{t.settings.env_save}</button
      >
      <button class="btn btn--ghost" id="env-clear-btn"
        >{t.settings.env_clear}</button
      >
    </div>
  </div>

  <!-- Local Routes -->
  <div class="settings-section" style="margin-top: 2rem;">
    <h3 class="settings-section-title">Rutas locales</h3>
    <p class="settings-hint">Carpetas locales para cada tipo de medio. Úsalas en la sección Biblioteca Local.</p>
    <div id="local-routes-list" style="display: flex; flex-direction: column; gap: 0.75rem; margin-top: 0.75rem;">
      <!-- Populated by JS -->
    </div>
  </div>

  <!-- Help Modals for Environment -->
  <div id="igdb-help-modal" class="settings-help-modal hidden">
    <div class="settings-help-modal-overlay"></div>
    <div class="settings-help-modal-content">
      <div class="settings-help-modal-header">
        <h3>¿Cómo obtener claves de IGDB?</h3>
        <button class="settings-help-modal-close" data-modal="igdb-help-modal">&times;</button>
      </div>
      <div class="settings-help-modal-body">
        <p><strong>IGDB</strong> es una base de datos de videojuegos. Sus claves se generan desde la consola de desarrolladores de <strong>Twitch</strong> (IGDB pertenece a Twitch/Amazon), así que necesitas una cuenta de Twitch para registrar la app.</p>
        <p style="margin-top: 0.75rem; padding: 0.6rem 0.75rem; border-left: 3px solid var(--color-error, #ff6b6b); background: color-mix(in srgb, var(--color-error, #ff6b6b) 10%, transparent); border-radius: 4px; font-size: 0.9em;">
          ⚠️ <strong>Requisito:</strong> tu cuenta de Twitch necesita la <strong>autenticación en dos pasos (2FA)</strong> activada para poder crear aplicaciones en la consola de desarrolladores. Si no la tienes, Twitch te pedirá activarla antes de continuar.
        </p>
        <ol style="margin: 1rem 0 0 1.5rem; list-style: decimal;">
          <li>Inicia sesión con tu cuenta de Twitch en <a href="https://dev.twitch.tv/console/apps" target="_blank" rel="noopener noreferrer" style="color: var(--accent); text-decoration: underline;">dev.twitch.tv/console/apps</a></li>
          <li>Activa la 2FA en tu cuenta si aún no la tienes (Twitch te redirigirá a hacerlo si intentas registrar una app sin ella)</li>
          <li>Pulsa <strong>"Register Your Application"</strong></li>
          <li>Rellena el formulario:
            <ul style="margin: 0.4rem 0 0 1.25rem; list-style: disc;">
              <li><strong>Name:</strong> cualquier nombre, por ejemplo "Metadea"</li>
              <li><strong>OAuth Redirect URLs:</strong> pon exactamente <code style="background: var(--bg-elevated); padding: 0.1rem 0.35rem; border-radius: 3px;">http://localhost</code> — no hace falta ningún puerto ni ruta adicional</li>
              <li><strong>Category:</strong> "Application Integration" (o similar)</li>
            </ul>
          </li>
          <li>Guarda la aplicación. Entra en ella y copia el <strong>Client ID</strong> que aparece</li>
          <li>Pulsa <strong>"New Secret"</strong> para generar el <strong>Client Secret</strong> y cópialo también (solo se muestra una vez)</li>
          <li>Pega ambas claves en los campos de arriba</li>
        </ol>
        <p style="margin-top: 1rem; font-size: 0.9em; color: var(--text-muted);">🔒 Nunca compartas tu Client Secret con nadie ni lo pegues en sitios públicos (chats, capturas de pantalla, repositorios) — con él se puede hacer pasar por tu aplicación. Metadea lo guarda solo en tu equipo, no se envía a ningún servidor propio.</p>
        <p style="margin-top: 0.5rem; font-size: 0.9em; color: var(--text-muted);">Más detalle oficial en <a href="https://api-docs.igdb.com/#getting-started" target="_blank" rel="noopener noreferrer" style="color: var(--accent); text-decoration: underline;">api-docs.igdb.com</a>.</p>
      </div>
    </div>
  </div>

  <div id="tmdb-help-modal" class="settings-help-modal hidden">
    <div class="settings-help-modal-overlay"></div>
    <div class="settings-help-modal-content">
      <div class="settings-help-modal-header">
        <h3>¿Cómo obtener claves de TMDB?</h3>
        <button class="settings-help-modal-close" data-modal="tmdb-help-modal">&times;</button>
      </div>
      <div class="settings-help-modal-body">
        <p><strong>TMDB</strong> (The Movie Database) es una base de datos de películas y series. Para obtener tus claves:</p>
        <ol style="margin: 1rem 0 0 1.5rem; list-style: decimal;">
          <li>Ve a <a href="https://developer.themoviedb.org/docs/getting-started" target="_blank" rel="noopener noreferrer" style="color: var(--accent); text-decoration: underline;">developer.themoviedb.org</a></li>
          <li>Sigue la guía de "Getting Started" en la documentación</li>
          <li>Necesitarás crear una cuenta si no la tienes</li>
          <li>Obtén tu <strong>API Key</strong> y <strong>Access Token</strong></li>
          <li>Pega ambas claves en los campos correspondientes</li>
        </ol>
        <p style="margin-top: 1rem; font-size: 0.9em; color: var(--text-muted);">Nota: Ambos campos son necesarios para acceso completo a TMDB</p>
      </div>
    </div>
  </div>

  <div id="anilist-help-modal" class="settings-help-modal hidden">
    <div class="settings-help-modal-overlay"></div>
    <div class="settings-help-modal-content">
      <div class="settings-help-modal-header">
        <h3>¿Cómo obtener la clave de AniList?</h3>
        <button class="settings-help-modal-close" data-modal="anilist-help-modal">&times;</button>
      </div>
      <div class="settings-help-modal-body">
        <p><strong>AniList</strong> se usa para iniciar sesión y sincronizar tu lista de anime/manga — la búsqueda de anime y manga funciona sin esta clave, solo la necesitas si quieres conectar tu cuenta de AniList.</p>
        <ol style="margin: 1rem 0 0 1.5rem; list-style: decimal;">
          <li>Inicia sesión en <a href="https://anilist.co/settings/developer" target="_blank" rel="noopener noreferrer" style="color: var(--accent); text-decoration: underline;">anilist.co/settings/developer</a></li>
          <li>Pulsa <strong>"Create New Client"</strong></li>
          <li>Ponle cualquier nombre, por ejemplo "Metadea"</li>
          <li>En <strong>"Redirect URL"</strong> pon exactamente <code style="background: var(--bg-elevated); padding: 0.1rem 0.35rem; border-radius: 3px;">https://anilist.co/api/v2/oauth/pin</code> — es la URL oficial de AniList para apps sin servidor propio; muestra el código en pantalla para que lo copies a mano</li>
          <li>Guarda el cliente y copia el <strong>Client ID</strong> que te asigna</li>
          <li>Pégalo en el campo de arriba</li>
        </ol>
        <p style="margin-top: 1rem; font-size: 0.9em; color: var(--text-muted);">Al pulsar "Conectar cuenta" en Conexiones, se abrirá una pestaña de AniList donde aceptas el acceso y te muestra un código (PIN) — pégalo de vuelta en Metadea para completar la conexión.</p>
      </div>
    </div>
  </div>

  <div id="steam-help-modal" class="settings-help-modal hidden">
    <div class="settings-help-modal-overlay"></div>
    <div class="settings-help-modal-content">
      <div class="settings-help-modal-header">
        <h3>¿Cómo obtener clave de Steam?</h3>
        <button class="settings-help-modal-close" data-modal="steam-help-modal">&times;</button>
      </div>
      <div class="settings-help-modal-body">
        <p><strong>Steam</strong> es la plataforma de distribución de videojuegos de Valve. Para obtener tu clave API:</p>
        <ol style="margin: 1rem 0 0 1.5rem; list-style: decimal;">
          <li>Necesitas una <strong>cuenta de Steam</strong> con historial de compras</li>
          <li>Ve a <a href="https://steamcommunity.com/dev/apikey" target="_blank" rel="noopener noreferrer" style="color: var(--accent); text-decoration: underline;">steamcommunity.com/dev/apikey</a></li>
          <li>Inicia sesión si es necesario</li>
          <li>Introduce un <strong>nombre de dominio</strong> (puede ser localhost para desarrollo local)</li>
          <li>Lee y acepta el acuerdo de licencia</li>
          <li>Se generará tu <strong>API Key</strong> automáticamente</li>
          <li>Copia la clave y pégala en el campo de abajo</li>
        </ol>
        <p style="margin-top: 1rem; font-size: 0.9em; color: var(--text-muted);">⚠️ No compartas tu API Key con otros; es personal y única para tu cuenta</p>
      </div>
    </div>
  </div>
</div>
