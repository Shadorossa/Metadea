---
import '../styles/pages/media.css';
import '../styles/pages/character.css';
import '../styles/pages/notifications.css';
import BaseLayout from '../layouts/BaseLayout.astro';
import { getLang, useTranslations } from '../i18n/index';
import { NotificationsPanel } from '../components/notifications/NotificationsPanel';

const lang = getLang(Astro.url);
const t    = useTranslations(lang);
---

<BaseLayout title={`${t.notifications.title} — Metadea`} titleKey="notifications.title" lang={lang} activePage="notifications">
  <NotificationsPanel client:load i18n={{ media: t.media, discord: t.discord, notifications: t.notifications, admin: t.admin }} />
</BaseLayout>
